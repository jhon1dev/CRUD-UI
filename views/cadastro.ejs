<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Cadastro de Aluno</title>
</head>
<body>
  <h1>Cadastro de Aluno</h1>
  <form method="POST" action="/salvar">
    <input type="hidden" name="id" value="<%= aluno ? aluno.id : '' %>">
    <label for="nome">Nome:</label>
    <input type="text" name="nome" id="nome" value="<%= aluno ? aluno.nome : '' %>"><br>
    <label for="dataNascimento">Data de Nascimento:</label>
    <input type="date" name="dataNascimento" id="dataNascimento" value="<%= aluno ? aluno.dataNascimento : '' %>" required min="1980-01-01" Max="2018-01-01"><br>
    <label for="matricula">Matrícula:</label>
    <input type="text" name="matricula" id="matricula" value="<%= aluno ? aluno.matricula : '' %>"><br>
    <label for="status">Status:</label>
    <select name="status" id="status">
      <option value="Ativo" <% if (aluno && aluno.status === 'Ativo') { %>selected<% } %>>Ativo</option>
      <option value="Inativo" <% if (aluno && aluno.status === 'Inativo') { %>selected<% } %>>Inativo</option>
    </select><br>
    <label for="email">E-mail:</label>
    <input id="email" type="text" name="email" value="<%= aluno ? aluno.email : '' %>" required><br>
    <button type="submit">Salvar</button>
    <a href="/">Cancelar</a>
  </form>
</body>
</html>
